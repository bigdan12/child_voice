# 攻防一体的童声语音系统

> 面向童声场景的语音生成与鉴别系统，覆盖音色转换、语音合成、伪造检测、文本童声化与真实度控制等关键模块。

## 项目简介

本项目围绕**童声场景**构建了一套**攻防一体的语音系统**。  
在攻击侧，我们重点研究：

- **童声音色转换 / 变声器**
- **童声语音合成大模型**

在防御侧，我们构建了：

- **面向童声场景的伪造语音鉴别器**

此外，我们还探索了两个辅助模块：

- **文本童声化**
- **真实度控制模型**

相比成人语音场景，童声数据具有更强的特殊性，包括发音不稳定、韵律异常、停顿边界不清晰、音高波动更大等问题。因此，常规语音生成与检测方法难以直接迁移到童声场景。本项目针对这些问题设计了相应的优化方案，并在多个指标上取得了较好的结果。

---

## 系统概览

本项目主要包含以下 5 个子方向：

1. **童声音色转换 / 变声器**
2. **童声语音合成大模型**
3. **童声伪造语音鉴别器**
4. **文本童声化**
5. **真实度控制模型**

---

## 1. 童声音色转换 / 变声器

### 背景

音色转换（Voice Conversion, VC）旨在在保持原始语义内容不变的前提下，将输入语音转换为目标说话人的音色。  
该能力既可直接用于**变声器**，也可作为后续语音合成系统中的关键组成部分，例如用于 **token-to-mel** 阶段的音色建模。

### 方法框架

我们构建了一个**基于 Flow Matching 的音色转换系统**。

### 核心问题

现有方法存在明显的 **train-test mismatch**：

- **训练阶段**：内容和音色通常来自同一条语音
- **测试阶段**：内容来自输入源（成人语音），音色来自目标说话人（童声）

这种不一致会导致模型在实际推理中难以兼顾：

- 内容准确性
- 音色相似度
- 生成稳定性

### 优化方案

#### Step 1：成人 + 童声数据联合训练

使用**成人数据与童声数据联合训练 Flow Matching 模型**，缓解成人输入到童声输出时的分布偏移问题。

#### Step 2：引入其他 VC 模型生成数据进行蒸馏训练

我们进一步引入其他 VC 模型生成的伪造数据，并设计蒸馏式训练策略：

- 增加一个 **fake 内容编码器**
- 使用生成音频对应的内容特征作为 fake 编码器输入
- 使用原始内容编码器输出作为指导信号
- 在原始 loss 基础上，增加两个 Flow Matching 输出之间的一致性 / 对比损失

### 实验结果

实验表明，在内容准确性和音质基本保持不变的情况下，我们的方法能够显著提升**音色相似度**。

| 模型 | CER | SSIM | Distill MOS | SRMR |
|---|---:|---:|---:|---:|
| freevc | 8.768 | 40.577 | 3.479 | 11.384 |
| dddmvc | 23.464 | 47.293 | 3.175 | 12.169 |
| meanvc | 8.182 | 28.87 | 3.875 | 11.25 |
| seedvc | 5.502 | 56.44 | 3.517 | 12.413 |
| cosyvoice2-vc | 6.949 | 49.507 | 3.608 | 14.518 |
| cosyvoice2-ft | 6.32 | 50.2 | 3.597 | 14.612 |
| cosyvoice3-vc | 5.915 | 56.67 | 3.55 | 12.359 |
| cosyvoice3-ft | 5.4 | 57.738 | 3.538 | 13.851 |
| cosyvoice3-spk_shift | 4.895 | 57.067 | 3.542 | 14.182 |
| **cosyvoice3-distill** | **5.679** | **60.48** | **3.552** | **14.247** |

### 小结

基于 Flow Matching 的蒸馏增强方案能够有效缓解训练-测试不一致问题，并显著提升童声音色转换的相似度表现。

---

## 2. 童声语音合成大模型

### 背景

语音合成大模型通过输入文本和目标音色，直接生成对应语音，是当前最灵活、最易使用的语音伪造方式之一。  
但目前**缺乏专门针对童声场景的多说话人语音合成方案**。

### 挑战

相比成人语音，童声数据存在大量发音与韵律缺陷，例如：

- 奶声奶气，发声状态不稳定
- 拖音明显
- 局部词或音节重复
- 停顿过多或断句不合理
- 咬字不清、发音含混
- 语速波动大
- 韵律边界弱
- 情绪化和表演化更强
- 音高变化更剧烈
- 轻声、气声、漏气现象较多

这些问题会导致大模型微调后容易出现：

- 对齐失败
- 生成过长或过短
- 重复生成
- 无法停止
- 内容幻觉

我们尝试过在多个大模型上进行童声微调，包括：

- Spark-TTS
- Fish-Speech
- FireRedTTS2

均观察到不同程度的对齐问题。

### 合成框架

我们基于 **Qwen2.5-0.5B** 构建童声语音合成模型，并在框架中增加：

- **spk-emb**：用于控制音色
- **style prompt**：用于控制童声风格

其余结构与 **CosyVoice3** 保持一致。

### 优化方案

#### Step 1：对数据进行分词与停顿边界打标

- 使用 **LTP** 生成分词边界
- 使用 **MFA** 生成停顿边界

目标是增强模型的分词断句能力，缓解童声数据中边界模糊导致的对齐困难。

#### Step 2：使用大规模中文数据保持基础对齐能力

基于 CosyVoice3 的 PTM，使用 **WenetSpeech4** 数据继续训练：

- Premium：945 小时
- Standard：4056 小时

目标是保持模型稳定的中文分词和断句能力，避免在童声小数据微调时损失基础语言建模能力。

#### Step 3：联合 VC 数据进行微调，实现音色与风格解耦

在第二步模型基础上进一步使用 **VC 数据**进行 Finetune：

- `spk-emb` 使用当前音频自身提取的说话人表示
- `style prompt`：
  - VC 数据使用输入源的风格
  - 真实录音使用自身风格

由于 VC 数据保留了源语音的发音风格，同时迁移了目标音色，因此可用于帮助模型学习：

- `spk-emb` 控制音色
- `style prompt` 控制童声风格

### 实验结果

结果表明：

- 使用 `norm_style` 时，模型在**内容准确度**和**音色相似度**上均超过当前主流大模型
- 使用 `child_style` 时，虽然内容准确度有所下降，但生成语音更接近目标童声风格与童声音色

| 模型 | CER | SIM | Distill MOS | SRMR |
|---|---:|---:|---:|---:|
| sparktts | 17.632 | 45.138 | 3.289 | 14.133 |
| fireredtts2 | 12.652 | 50.26 | 3.405 | 14.158 |
| indextts2 | 11.894 | 62.99 | 3.466 | 12.746 |
| cosyvoice2 | 14.591 | 57.15 | 3.712 | 15.905 |
| cosyvoice2-ft | 21.056 | 55.245 | 3.746 | 16.211 |
| cosyvoice3 | 9.473 | 63.372 | 3.8 | 14.626 |
| ft-child | 18.356 | 64.301 | 3.56 | 15.028 |
| ltp-ft_child | 19.121 | 65.165 | 3.563 | 14.889 |
| **our-norm_style** | **8.978** | **65.574** | 3.51 | 14.686 |
| **our-child_style** | 22.407 | **66.356** | 3.541 | 15.224 |

### 小结

我们提出的童声语音合成方案通过：

- 边界标注
- 大规模普通话继续训练
- VC 联合微调

有效缓解了童声场景下的大模型对齐困难，并实现了音色与风格的可控生成。

---

## 3. 童声伪造语音鉴别器

### 背景

当前伪造语音检测研究大多聚焦于成人场景，**缺乏专门针对童声场景的鉴别模型**。  
由于童声与成人语音在音高、发音稳定性、韵律结构等方面存在显著差异，成人场景的检测模型通常难以直接迁移。

### 方法

我们构建了一个面向童声场景的伪造语音鉴别系统，核心思路是：

- 使用多个 **vocoder** 生成伪造语音
- 使用多个 **codec** 生成伪造语音
- 将不同伪造路径的数据统一联合训练

从而增强模型对多种童声伪造形式的泛化能力。

### 训练数据

#### Vocoder 数据

| 模型名称 | 类别 | 数据量 |
|---|---|---|
| Griffin-Lim | vocoder | 120 小时 |
| WORLD | vocoder | 120 小时 |
| APNet2 | vocoder | 120 小时 |
| Basis-MelGAN | vocoder | 120 小时 |
| Vocos | vocoder | 120 小时 |
| HiFi-GAN | vocoder | 120 小时 |
| BigVGAN | vocoder | 120 小时 |

#### Codec 数据

| 模型名称 | 类别 | 数据量 |
|---|---|---|
| WavLM-Kmeans | codec | 120 小时 |
| WavTokenizer | codec | 120 小时 |
| SpeechTokenizer | codec | 120 小时 |
| SemantiCodec | codec | 120 小时 |
| Mimi | codec | 120 小时 |
| FocalCodec | codec | 120 小时 |
| Encodec | codec | 120 小时 |
| DAC | codec | 120 小时 |
| BigCodec | codec | 120 小时 |

### 实验结果

| 模型 | 自测集 EER | 说明 |
|---|---:|---|
| vocoder_w2v2aasist | 37.422 | 来自开源项目 CodecFake |
| codec_w2v2aasist | 12.203 | 来自开源项目 CodecFake |
| cotrain_w2v2aasist_CSAM | 10.978 | 来自开源项目 CodecFake |
| SpeechLLM-as-Judges | 39.21 | 大模型鉴别框架 |
| our-melLCNN | 11.2 | 自建数据训练 |
| our-w2vLCNN | 0.69 | 自建数据训练 |
| **our-w2vAASIST** | **0.193** | 自建数据训练 |

### 小结

基于多种伪造数据联合训练的童声鉴别器，在自测集上取得了显著优于开源方法的效果。其中 `our-w2vAASIST` 达到了 **0.193 EER**。

---

## 4. 文本童声化

### 背景

为了使合成语音在语言表达层面更符合儿童说话习惯，我们设计了一个文本童声化模块，将普通文本改写为更符合童声风格的表达。

### 方法

整体流程如下：

1. 使用 **Qwen3-8B** 生成童声化文本
2. 使用 **LLaMA-8B** 对生成结果进行筛选
3. 使用筛选后的高质量数据训练 **Qwen3-0.6B** 模型

### 结果

- `token-acc` 达到 **93.6%**

### 小结

文本童声化模块能够为后续语音合成提供更自然、更符合儿童表达习惯的语言输入。

---

## 5. 真实度控制模型

### 目标

希望通过一个真实度控制模块，主动调节生成语音的真实感程度，为伪造语音研究提供更细粒度的可控能力。

### 尝试方向

我们分别尝试了以下三类训练框架：

- **声码器框架**
- **音色转换框架**
- **语音增强框架**

### 当前结果

目前三个方向均尚未达到理想效果。  
其中表现最好的方向是**语音转换框架**：

- 在我们自研的童声鉴别器上，尚未带来明显的真实度评分提升
- 但在开源鉴别器上，真实度评分约提升了 **20 个百分点**

### 小结

当前真实度控制模块仍在探索阶段，后续需要进一步增强其对童声高阶伪造痕迹的建模能力。

---

## 结果总结

### 攻击侧

- **音色转换**：在保持内容准确度和音质的同时，显著提升童声音色相似度
- **语音合成**：在童声场景下实现了更好的内容控制、音色相似度和风格可控性
- **文本童声化**：提供了更符合儿童表达习惯的文本输入

### 防御侧

- 构建了针对童声场景的伪造语音鉴别器
- 在自测集上取得 **0.193 EER** 的较强检测效果


---

## 后续工作

- 文本童声化效果评测
- 真实度控制模型继续优化

