## 背景与动机（Why）

当前语音鉴别模型的主流做法，是直接使用 **SSL（自监督学习）特征**作为特征提取器。但许多工作仍把 SSL 当作“黑盒”，只使用**最后一层输出**，而没有充分挖掘不同层的语义含义差异。

近期**语音生成**领域的论文给了一个关键启示：**SSL 的不同层蕴含的信息含义截然不同**。  
- **第六层特征**更偏向于 **音色/风格（timbre & style）**；  
- **最后一层特征**更偏向于 **语义（content/semantics）**。

对于语音鉴伪任务而言，伪造痕迹往往并不直接破坏语义，而更容易体现在 **声码器残留、频带断裂、局部风格不自然**等层面——这些更接近“音色与局部风格”的失配，而不是语义层面的错误。

因此，相比最后一层，**第六层特征可能对鉴伪更具判别力**。这也是我们方案命名为 **TAB-6（Timbre-Aware Bias from 6th layer）** 的核心出发点。

---

## 主流方法与不足

当前主流的 **ASSIST** 方案，是对同一个 SSL 特征分别构建 **频谱图（频域）**和 **时谱图（时域）**，再通过 **Attention** 机制进行聚合。

我们认为这种方式更多是“**把特征转成不同视图后再融合**”，但没有深入挖掘时域与频域之间更深层的互补逻辑。  
为了更直观地说明我们的思路，我们可以类比：对一个人的评价通常包含两种维度。

- **时域分析**像是在看这个人的  
  **“日常行为轨迹”**：关注局部的、连续的、微观的变化；例如他每天几点出门、说什么话。

- **频域分析**像是在评估这个人的  
  **“整体性格底色”**：跳出具体时间线，关注周期性与全局规律；例如他更外向还是内向、节奏更快还是更慢。

对于鉴伪来说：  
真人的“日常轨迹”和“性格底色”是高度协调的；而伪造语音可能在局部时序上模仿得很像（轨迹对），但在周期性规律上会出现破绽（底色错）。  
因此，我们需要一种能够**深度融合这两种评价维度**的建模方式。

---

## 实现方案（How）

### 1）输入源：舍弃最后一层，采用第 6 层
我们不使用 SSL 最后一层，而是直接采用 **第 6 层特征**作为基础输入，以更好捕获音色与风格相关的判别信息。

---

### 2）时域分支：TCN 提取 1D 局部动态
- 使用典型的 **TCN（Temporal Convolutional Network）**模块处理 1D 时序信息；
- TCN 擅长捕捉局部、短期依赖模式；
- 这对应“日常行为轨迹”的建模目标。

---

### 3）频域分支（核心创新）：按周期拆分形成 2D 建模
我们不简单地做 STFT，而是将 1D 特征按**不同周期**拆分并重排成 **2D 结构**进行建模。

- 这样做的好处是：我们可以把“周期内的结构模式”显式地呈现出来；
- 同时可以借助图像领域成熟的 **2D 卷积类模型能力**提取周期特征；
- 之后我们再通过 **Attention** 将这些周期相关表征重新聚合回时间维度；
- 这对应“整体性格底色”的建模目标。

---

### 4）时频融合：Self-Attention 联合判别局部与全局
最后，我们通过 **Self-Attention** 将：
- 负责局部细节的 **TCN 输出**，
- 负责全局周期规律的 **频域输出**，
进行联合聚合。

Self-Attention 能让模型在每个时间步上动态权衡：  
当前时刻的局部波形变化，与其所属的整体周期风格是否一致。  
从而实现对伪造痕迹的**精准捕捉**。

---

## 实验结果（Results）

我们在 **codecfake** 系列实验配置下进行对比，结果如下：

| 方法 | LA19-test | LA21-test |
|---|---:|---:|
| paper-result | 0.12 | - |
| ssl-24 | 3.82 | 14.431 |
| ssl-6 (复现)| 0.118 | 11.5 |
| TAB-24 | 1.929 | 11.337 |
| **TAB-6** | **0.108** | **10.66** |

综合来看：  
- 仅使用第 6 层（ssl-6）相较第 24 层（ssl-24）已有明显优势；  
- 在引入时域-频域联合建模后，**TAB-6 进一步取得最好表现**（LA19 更低、LA21 也保持领先）。

---

## 小结（Conclusion）

本工作通过两个关键设计提升语音鉴伪能力：  
1. **TAB-6 利用 SSL 第 6 层的音色/风格信息**，与鉴伪痕迹更匹配；  
2. **时域-频域联合建模**，从“日常轨迹 vs 性格底色”的角度进行深度融合，从而提升对周期性伪造失配的捕获能力。
